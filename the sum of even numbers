# Указываем значения для k и n
k = 5
n = 15

# Инициализируем переменную суммы нечетных чисел
sum_odd = 0

# Используем цикл while для прохода по числам от k до n
while k <= n:
  # Проверяем, является ли число нечетным
  if k % 2 != 0:
    # Если число нечетное, то добавляем его к сумме
    sum_odd += k
  # Увеличиваем значение k на 1
  k += 1

# Выводим полученную сумму
print("Сумма нечетных чисел от", k, "до", n, "равна", sum_odd)
